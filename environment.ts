// environment.ts (substitua os valores pelas URLs reais da sua API)
export const environment = {
  production: false,
  apiBase: 'https://api.exemplo.com', // <-- COLE AQUI A BASE API (ex: https://backend.seu-dominio.com)
  exchangePath: '/exchange',          // caminho opcional para câmbio (ajuste se necessário)
  transactionsPath: '/transactions'   // caminho opcional para extrato (ajuste se necessário)
};
